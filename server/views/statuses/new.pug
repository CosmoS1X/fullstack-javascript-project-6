extends ../layouts/application.pug

block header 
  = t('layouts.headers.statusCreation')

block content 
  +formFor()(action='/statuses' method='post')
    +input(status, 'name', errors)(type='text')
    +submit(status)(value=t('layouts.buttons.create'))
