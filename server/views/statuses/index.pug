extends ../layouts/application.pug

block header 
  = t('layouts.headers.statuses')

block content 
  a.btn.btn-primary(href= route('newStatus'))= t('layouts.buttons.createStatus')
  - cols = ['id', 'name', 'createdAt']
  +table('status', cols, statuses)
